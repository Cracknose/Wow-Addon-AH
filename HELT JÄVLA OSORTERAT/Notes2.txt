NOTES



 = GetNumAuctionItems("list")    ===  50

CanSendAuctionQuery()  ===  1



name, texture, count, quality, canUse, level, minBid, minIncrement,buyoutPrice, bidAmount, highestBidder, owner, sold = GetAuctionItemInfo("list", index)


name, texture, count, quality, canUse, level, minBid, minIncrement,buyoutPrice, bidAmount, highestBidder, owner, sold = 















local MyFrame = CreateFrame("Frame")

function MyEventListener(e)
  DEFAULT_CHAT_FRAME:AddMessage(GREEN_FONT_COLOR_CODE .. e)
end


QueryAuctionItems("", 0, 0, 0, 0 ,0 ,0 ,0 ,0)

name, texture, count, quality, canUse, level, minBid, minIncrement,buyoutPrice, bidAmount, highestBidder, owner, sold = GetAuctionItemInfo("list", 3)

DEFAULT_CHAT_FRAME:AddMessage(GREEN_FONT_COLOR_CODE .. name)

MyFrame:RegisterEvent("AUCTION_ITEM_LIST_UPDATE")
MyFrame:RegisterScript("MyEventListener")

